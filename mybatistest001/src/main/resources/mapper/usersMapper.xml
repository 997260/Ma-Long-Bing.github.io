<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hellojava.dao.UsersMapper">

    <select id="loadAll" resultType="com.hellojava.entity.Users">
        select * from users
    </select>

    <select id="loadById" resultType="com.hellojava.entity.Users" parameterType="int">
        select * from users where usersId = #{usersId}
    </select>
</mapper>